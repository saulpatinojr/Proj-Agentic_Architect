{
  "name": "@code-conductor/root",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build:ts && npm run build:vscode",
    "clean": "tsc -b --clean",
    "typecheck": "tsc -b --pretty false",
    "test": "vitest run",
    "test:watch": "vitest",
    "cc:validate": "npm run build && node packages/cli/dist/index.js validate .",
    "cc:doctor": "npm run build && node packages/cli/dist/index.js doctor .",
    "cc:plan": "npm run build && node packages/cli/dist/index.js plan --repo . --risk R1 --objective 'Validate Code Conductor planning'",
    "build:ts": "tsc -b",
    "build:vscode": "node scripts/build-vscode.mjs",
    "package:vscode": "npm run build && node scripts/package-vscode.mjs",
    "test:packaged": "node scripts/test-packaged-planning.mjs"
  },
  "devDependencies": {
    "@types/node": "^26.6.1",
    "typescript": "^7.0.2",
    "vitest": "^5.0.1",
    "esbuild": "0.28.2",
    "@vscode/vsce": "4.0.0"
  }
}
